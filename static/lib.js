function sortTable(column) {
  /* rebuild the table and replace the existing table with the newly
  re-ordered one. */


   // column is a node element
  colIndex = 0 + column.getAttribute("id");

  // get all the table rows
  table = column.parentElement.parentElement.parentElement;
  rows = table.getElementsByTagName("tr");
  header = rows[0];

  var newRowsOrder = Array();
  newRowOrder[0] = rows

  for (row in rows) {
    row
  }

}